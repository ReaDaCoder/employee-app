Initialize the system
    Display Login Page
    If Login Successful Then
        Display Admin Dashboard
    Else
        Display Error Message
    End If
End Function
Initialiaze employeeDetails variable
let employeeDetails = []
Employee details will be contained in an array of objects
Accept users input and push into array
push() metthod will be used to take in user's information